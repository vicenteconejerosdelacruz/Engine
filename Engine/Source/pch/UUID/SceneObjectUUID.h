#pragma once

#include <memory>
#include <UUID.h>

DEF_SUUUID_TYPE(Scene, Renderable, GetRenderableSceneObject)
DEF_SUUUID_TYPE(Scene, Camera, GetCameraSceneObject)
DEF_SUUUID_TYPE(Scene, Light, GetLightSceneObject)
DEF_SUUUID_TYPE(Scene, SoundFX, GetSoundFXSceneObject)
